name: example
description: "A new Flutter project."
publish_to: "none" #
version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.20.2
  
  offline_db:
    path: ../

  redis: ^4.0.0
  awesome_task_manager: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
flutter:
  uses-material-design: true
